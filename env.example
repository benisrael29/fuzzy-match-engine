# MySQL Credentials
# These are used when mysql_credentials references environment variables in config
MYSQL_HOST=localhost
MYSQL_USER=myuser
MYSQL_PASSWORD=mypassword
MYSQL_DATABASE=mydatabase

# AWS S3 Credentials (optional - will use IAM role if not provided)
# These are used when s3_credentials references environment variables in config
# If not set and s3_credentials is omitted, the system will use IAM role automatically
AWS_ACCESS_KEY_ID=your_access_key_id
AWS_SECRET_ACCESS_KEY=your_secret_access_key
AWS_REGION=us-east-1

# Usage:
# 1. Copy this file to .env in your project root
# 2. Replace the placeholder values with your actual credentials
# 3. Reference them in your config file using ${VAR_NAME} syntax
# 4. Example: "password": "${MYSQL_PASSWORD}"
# 5. For default values: "host": "${MYSQL_HOST:localhost}"

